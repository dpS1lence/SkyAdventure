using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraCrowch : MonoBehaviour
{
    /*public GameObject cameraFreez;
    public GameObject cameraDown;

    void Update()
    {
        if(Input.GetKeyDown(KeyCode.LeftControl))
        {
            cameraFreez.transform.position = cameraDown.transform.position;
        }
        if(Input.GetKeyUp(KeyCode.LeftControl))
        {
            cameraFreez.transform.position = cameraFreez.transform.position;
        }
    }*/
}
